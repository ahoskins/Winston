"""
Important
---------

When testing a remote database connection, 
**use the remote database factory to create it**.

Iterate through the institution config files.

If an institution uses the db type of interest,
create an instance of it and briefly test its functionality.
"""
